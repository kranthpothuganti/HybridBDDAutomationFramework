name: Run Hybrid BDD Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0' # Update this to match your SDK version

      - name: Restore dependencies
        run: dotnet restore HybridTestFramework/HybridTestFramework.sln

      - name: Build solution
        run: dotnet build HybridTestFramework/HybridTestFramework.sln --configuration Release

      - name: Run tests and generate LivingDoc
        run: |
          dotnet tool restore
          dotnet test HybridTestFramework/WebTests/WebTests.csproj --logger "html;LogFileName=TestResults.html"
          dotnet livingdoc feature-folder HybridTestFramework/WebTests/Features --output HybridTestFramework/WebTests/LivingDoc.html
          
      - name: Upload test report
        uses: actions/upload-artifact@v4
        with:
          name: TestReport
          path: WebTests/LivingDoc.html
